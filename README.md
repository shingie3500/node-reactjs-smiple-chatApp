# ChatApp Using React.js(frontend) && Socket.io and Node.js(backend)

## Installation

1. Run `cd chatApp && npm install` 

2. Run `cd client && npm install` 

3. Make sure you are in the chatApp folder 

4. Run `npm run dev` to start the chatApp
